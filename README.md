# mundaneb3at.github.io

GitHub Pages root for `mundaneb3at.github.io` — the account's default Pages domain also serves
every project page under it (e.g. `mundaneb3at.github.io/sim-maker-kit/`).

No custom domain is configured here (a `CNAME` file pointing to `stosnia.me` was removed
2026-08-17 — that domain has no server listening on it, and its presence was 301-redirecting
*every* Pages URL on this account, including the sim-maker-kit demo, to a dead endpoint). Re-add a
`CNAME` only once whatever `stosnia.me` should point to is actually live.

No other site content yet. No LICENSE (nothing to license yet).
